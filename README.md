problems:

1. Create a function that allows me to generate any type of loop I want, whether from small to large or large to small.

2. Create a function where if I input a subject mark, it will return the GPA and Grade.

3. Also, create a function where if I input six subjects’ results, it will return each subject’s GPA and Grade along with the final GPA and Grade.

4. Create a function to calculate the area of a rectangle, square, circle, and triangle.

5. Create a function to calculate BMI (Body Mass Index), which will also return health guidance according to the BMI result.

6. Create a function that can convert any amount of currency into Bangladeshi Taka. For currencies, you may use the Dollar, Pound, Euro, and Riyal.

7. Create a function that allows the user to generate a headline, paragraph, and button with full design customization options such as font family, font size, font color, font alignment, background color, and so on.

8. Create a function that takes an image link and returns the image with different customizable options such as width, height, border-radius, etc.

9. Create a function that takes a direct YouTube link and converts it into an embed link to play the video.

10. Create a function that returns styled messages for success, error, warning, and info.
